{% extends 'base.html.twig' %}

{% block body %}
{{form_start(form)}}
  {{form_widget(form)}}
  <button type="submit" name="button" class="btn btn-primary">{{ modif ? "Enregistrer" : "Modifier"}}</button>
{{form_end(form)}}
{% endblock %}
